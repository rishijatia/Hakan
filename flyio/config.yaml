# Hermes config for Fly.io gateway
model:
  default: "xiaomi/mimo-v2.5-pro"
  provider: "openrouter"
  base_url: "https://openrouter.ai/api/v1"

gateway:
  streaming:
    enabled: false

skills:
  enabled: true

