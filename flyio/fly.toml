app = 'hermes-gateway'
primary_region = 'fra'

[build]
  image = "nousresearch/hermes-agent:latest"

[processes]
  app = "gateway run"

[[mounts]]
  source = "data"
  destination = "/opt/data"

[[vm]]
  memory = "2gb"
  cpus = 1
